   {"emeraldwalk.runonsave": {
    "commands": [
        {
            "match": "\\.cpp$",
            "cmd": "g++ ${file} -o ${fileDirname}/${fileBasenameNoExtension}.exe && ${fileDirname}/${fileBasenameNoExtension}.exe"
        }
    ]
}
   }